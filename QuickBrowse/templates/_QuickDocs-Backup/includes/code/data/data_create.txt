$data = Array(
'name' => $_POST['form_name'],
'password' => $_POST['form_pass']
);

$id = $DATA->data_create(Array $data, String $table);

if(!$id){
	echo('[INFO]' . $DATA->INFO . '<br>');
	echo('[ERROR] '$DATA->ERROR);
}